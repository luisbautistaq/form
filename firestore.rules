/**
 * @file Overview
 * This ruleset grants public read and write access to all data in the Firestore database.
 *
 * Core Philosophy:
 * This ruleset is designed for rapid prototyping and allows all users, authenticated or not,
 * to read and write any data in the database.
 *
 * WARNING: This configuration is highly insecure and should NEVER be used in a production environment.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read/write access to all users on all documents
    match /{document=**} {
      allow read, write: if true;
    }
  }
}